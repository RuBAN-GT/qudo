#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler/setup'
require 'pathname'
require 'qudo/utils/file_loader'

Bundler.require(:default, :development)
Qudo::Utils::FileLoader.recursive_require Pathname.new(__dir__).join('..', 'lib', 'qudo')
Pry.start
